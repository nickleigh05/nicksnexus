---
import '../styles/global.css'

interface Props {
  title?: string
}

const { title = 'Nick Leigh' } = Astro.props
const description =
  'Nick Leigh is a computer science student at Arizona State who builds things from scratch to understand them properly — currently focused on cloud and AI/ML.'
const base = import.meta.env.BASE_URL.replace(/\/$/, '')
const year = new Date().getFullYear()
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#050208" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <title>{title}</title>
    <noscript>
      <style>
        [data-reveal],
        [data-hero] {
          opacity: 1 !important;
        }
      </style>
    </noscript>
  </head>
  <body class="bg-void font-sans text-ink antialiased">
    <!-- layer 1: live WebGL background, fixed behind everything -->
    <canvas id="webgl-bg" class="fixed top-0 left-0 z-0 h-screen w-screen" aria-hidden="true"
    ></canvas>
    <div class="grain" aria-hidden="true"></div>

    <!-- layer 2: UI overlay — inert by default so the canvas shows through;
         interactive elements opt back in with pointer-events-auto -->
    <div class="pointer-events-none relative z-10">
      <header class="fixed inset-x-0 top-0 z-20 px-4" data-hero="frame">
        <nav
          class="pointer-events-auto mx-auto mt-4 flex max-w-4xl items-center justify-between rounded-full glass px-6 py-3"
        >
          <a
            href={`${base}/`}
            class="font-display text-lg font-bold tracking-tight transition-colors duration-(--dur-micro) hover:text-neon-blue"
          >
            nick leigh<span class="text-neon">.</span>
          </a>
          <div class="flex items-center gap-5 text-sm sm:gap-7">
            <a href="#work" class="link-ink">work</a>
            <a href="#contact" class="link-ink">contact</a>
            <a
              href="https://github.com/nickleigh05"
              target="_blank"
              rel="noopener"
              class="link-ink"
            >
              github
            </a>
          </div>
        </nav>
      </header>

      <main class="mx-auto max-w-4xl px-6">
        <slot />
      </main>

      <footer
        class="mx-auto flex max-w-4xl items-baseline justify-between px-6 py-10 font-mono text-xs text-ink-soft"
      >
        <span
          >© {year} · <span title={`${year} in hexadecimal`}
            >0x{year.toString(16).toUpperCase()}</span
          ></span
        >
        <a
          href="https://github.com/nickleigh05"
          target="_blank"
          rel="noopener"
          class="pointer-events-auto link-ink"
        >
          github
        </a>
      </footer>
    </div>

    <script>
      import { initWebGLBackground } from '../scripts/webgl-bg'
      import '../scripts/motion'

      const canvas = document.getElementById('webgl-bg')
      if (canvas instanceof HTMLCanvasElement) initWebGLBackground(canvas)
    </script>
  </body>
</html>
